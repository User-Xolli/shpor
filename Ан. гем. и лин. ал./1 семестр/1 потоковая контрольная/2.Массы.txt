Центр.масс.мат.точек:
h1*m1=h2*m2
H/h2 = h1/h2+1 = m2/m1+1 = M/m1
H/h1 = M/m2
----
OC = ∑(mi*ri) / ∑(mi)
----
теор.
1)Центр масс не знавис.от нач.точки O
2)Замена подсист.на центр масс с массой подсистемы не изм.сист
----
Док-во:
1)OC = ∑(mi*ri)/∑(mi) = ∑(mi*(a+r'i))/∑(mi) = a+∑(mi*r'i)/∑(mi) = a+O'C'
2)OC=∑(mi*ri)/∑mi = (∑(mj*rj)+∑(mk*rk))/∑mi = (∑mj*∑(mj*rj)/∑mj+∑(mk*rk))/∑mi = (∑mj*OP + ∑(mk*rk))/∑mi